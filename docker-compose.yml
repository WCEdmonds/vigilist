services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: descubre
      POSTGRES_USER: descubre
      POSTGRES_PASSWORD: descubre_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
