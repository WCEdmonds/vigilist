# P4-1 SSO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **Note:** executed inline by the planning session against the spec's exact contracts.

**Spec:** `docs/superpowers/specs/2026-07-23-p4-1-sso-saml-design.md`

## Global Constraints
- Branch `feat/p4-1-sso-saml` off main. Migration `e7f8a9b0c1d2` on `d6e7f8a9b0c1`; purity; head re-check before PR.
- Enforcement is SERVER-side in get_current_user via a testable `enforce_org_sso` helper; creator_emails exempt (lockout escape hatch). Provider ids `^(saml|oidc)\.[A-Za-z0-9_-]+$`.
- Public sso-config endpoint discloses only provider_id/enforced/org_name.
- Tests fake-session, 0 warnings; frontend gate npm run build; no attribution trailers.

### Task 1: Migration + Organization columns. Commit.
### Task 2: `services/sso.py` (enforce_org_sso) + `GET /api/auth/sso-config` + wiring into get_current_user + `PUT /api/organizations/{slug}/sso` (new small router or existing productions/orgs surface; gate creator_emails; audit) + fake-session tests. Commit.
### Task 3: Frontend — useAuth.loginWithSSO (SAML/OIDC providers), AuthPage slug+email discovery, SSO button, enforced hiding. Build. Commit.
### Task 4: Full suite; fresh-main check; push; PR.
