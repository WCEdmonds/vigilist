rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /productions/{productionId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && productionId in request.auth.token.prods;
    }
  }
}
